diff -rupN vmdebootstrap-0.5/vmdebootstrap vmdebootstrap-0.5.new/vmdebootstrap
--- vmdebootstrap-0.5/vmdebootstrap	2014-11-16 16:10:24.000000000 +0100
+++ vmdebootstrap-0.5.new/vmdebootstrap	2016-09-13 15:13:34.689988211 +0200
@@ -33,8 +33,8 @@ __version__ = '0.5'
 class VmDebootstrap(cliapp.Application):
 
     def add_settings(self):
-        default_arch = subprocess.check_output(
-            ["dpkg", "--print-architecture"]).strip()
+        arch = subprocess.check_output(['uname', '-m']).strip()
+        default_arch = 'amd64' if arch == 'x86_64' else 'i386' if re.match('i[3-8]86', arch) else arch
 
         self.settings.boolean(['verbose'], 'report what is going on')
         self.settings.string(['image'], 'put created disk image in FILE',
